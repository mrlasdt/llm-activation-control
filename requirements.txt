# torch cu128 build: matches NVIDIA driver supporting CUDA up to 12.8.
# The +cu128 local tag forces the wheel from the PyTorch index below
# (the default PyPI torch ships a newer CUDA build that needs a newer driver).
--extra-index-url https://download.pytorch.org/whl/cu128

datasets==3.2.0
einops==0.8.1
fastapi==0.115.12
gradio==5.31.0
litellm==1.70.4
numpy==2.2.6
pandas==2.2.3
plotly==6.1.1
pydantic==2.11.5
#Requests==2.32.3
scikit_learn==1.6.1
#setuptools==75.1.0
torch==2.11.0+cu128
transformer_lens==2.15.4
transformers==4.48.0
#vllm==0.8.5.post1
